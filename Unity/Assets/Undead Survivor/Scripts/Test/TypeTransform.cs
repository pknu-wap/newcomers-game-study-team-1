using UnityEngine;

public class TypeTransform : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        uint a = 0, b = 2;
        Debug.Log(a + b - 2*b + 1);
    }
}
